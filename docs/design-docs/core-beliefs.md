# Core Beliefs

- The library should remain framework-light so host applications can choose their own persistence, supervision, and web layers.
- LTI handshake correctness and security outrank convenience APIs.
- Examples and documentation are part of the product surface because most users learn the package from repository docs.
